const PageLoader = () => {
    return (
        <main className="page-loader">
            <span className="page-loader__logo">PrepForge</span>
            <span className="spinner spinner--lg spinner--brand" />
        </main>
    );
};

export default PageLoader;
